rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Alle Collections unter gyms/{gymId}/...
    match /gyms/{gymId}/{document=**} {
      allow read, write: if request.auth != null
        && request.auth.token.gymId == gymId;
    }
  }
}
