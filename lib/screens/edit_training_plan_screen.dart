import 'package:flutter/material.dart';

class EditTrainingPlanScreen extends StatelessWidget {
  const EditTrainingPlanScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Edit Trainingsplan")),
      body: const Center(child: Text("Edit Trainingsplan - Stub")),
    );
  }
}
